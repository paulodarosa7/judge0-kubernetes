apiVersion: v1
kind: Service
metadata:
  name: judge0-service

spec:
  type: ClusterIP

  selector:
    app: srv-judge0

  ports:
    - name: http
      protocol: TCP
      port: 2358
      targetPort: 2358


---

apiVersion: v1
kind: Service
metadata:
  name: redis

spec:
  type: ClusterIP

  selector:
    app: redis

  ports:
    - name: redis
      protocol: TCP
      port: 6379
      targetPort: 6379kind: Service
metadata:
  name: judge0-redis
spec:
  ports:
    - port: 6379
  selector:
    app: redis-judge0
